//** Theme */
import { colors } from "@/themes/color";

export const FooterStyles = {
    Container: {
        backgroundColor: colors.primary,
        p: 3,
        textAlign: "center",
        color: colors.white,
    },
};
