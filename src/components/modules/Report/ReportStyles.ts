import { colors } from "@/themes/color";

export const ReportStaffStyles = {
    Add: {
        backgroundColor: colors.primary,
        color: colors.white,

        "&:hover": {
            backgroundColor: colors.primary300,
        },
    },
};
