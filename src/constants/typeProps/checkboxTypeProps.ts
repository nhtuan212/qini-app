export enum CheckboxSize {
    small = "small",
    medium = "medium",
}

export enum CheckboxColor {
    primary = "primary",
    secondary = "secondary",
    error = "error",
    warning = "warning",
    info = "info",
    success = "success",
}
