export enum AlertColor {
    warning = "warning",
    success = "success",
    error = "error",
    info = "info",
}

export enum AlertVariant {
    standard = "standard",
    filled = "filled",
    outlined = "outlined",
}

export enum AlertSeverity {
    error = "error",
    warning = "warning",
    info = "info",
    success = "success",
}

export enum AlertVertical {
    top = "top",
    bottom = "bottom",
}

export enum AlertHorizontal {
    center = "center",
    right = "right",
    left = "left",
}
